---
name: chat-log-analysis
description: 分析 QQ/微信等聊天记录导出文件（JSONL/JSON/CSV），产出有数据、有判断、有结论的 Markdown 分析报告，可选生成 matplotlib 图表和 PPT，并可进一步生成带数据可视化、照片滚动故事线、音乐/蛋糕/彩蛋互动的 HTML 祝福网页（生日/纪念日/告白礼物）。当用户提供聊天记录文件并要求「分析聊天记录」「做聊天数据可视化」「生成聊天报告」「分析关系/情绪/称呼演变」「做纪念日/生日礼物网页」等场景时使用。
agent_created: true
---

# 聊天记录分析报告

把导出的聊天记录（JSONL/JSON/CSV）转化为一份有数据、有判断、有结论的 Markdown 报告，可选生成图表和 PPT，并可进一步生成带互动的 HTML 祝福网页。

## 何时使用

- 用户提供 QQ / 微信 / 其他 IM 的聊天记录导出文件，要求做分析或可视化
- 用户想了解「聊了多少」「怎么聊」「聊什么」「关系如何变化」等
- 用户要求分析特定维度：情绪、冲突、称呼演变、表白节点等
- 用户要求把分析结果 + 共同照片做成 HTML 网页，作为生日/纪念日/告白礼物

## 核心流程

### 第 1 步：数据获取（优先现成工具，勿自行解密）

- **聊天记录优先用现成导出工具**：QQ 用 `QQChatExporter`（GitHub: shuakami/qq-chat-exporter，直接导出 JSONL）；微信用各类导出工具。几秒到几分钟即可拿到可读数据。
- **切勿自己解密 NTQQ 加密数据库**：NTQQ 数据库是 SQLCipher 加密，密钥在进程内存（`wrapper.node`），解密是深坑，自己写解密器 + 暴力搜参数会浪费大量时间。遇到用户只给了加密 `.db` 文件时，直接推荐现成工具（`QQChatExporter` / NapNeko `qq_dump_db`），不要造轮子。

### 第 2 步：数据探查

- 先读导出目录里的 `manifest.json`（如有），了解总消息数、时间范围、消息类型、双方身份。
- 统计 `type` 字段取值分布、`sender` 分布、`recalled`/`system` 数量。
- **关键坑**：图片消息可能没有独立 type，藏在 `type="text"` 的 `content.text` 里，以 `[图片:xxx]` 占位符表示；表情是 `type_17`（贴纸）、通话是 `type_19`（含时长）、`[回复消息]` 是引用前缀。分类时要按占位符前缀识别。

### 第 3 步：数据清洗

- 排除 `type == "system"` 的消息和 `recalled == true` 的消息，得到「有效消息」。
- 时间戳通常为 UTC 毫秒，转北京时间加 8 小时：`datetime.fromtimestamp(ts/1000, tz=timezone.utc) + timedelta(hours=8)`。

### 第 4 步：统计分析（按 SOP 口径）

用 `scripts/analyze.py`（通用模板，需按实际字段微调）。核心口径：

- **基础**：总消息数、活跃天数（自然日去重）、双方占比、类型构成。
- **时间**：月双折线、24 小时活跃曲线（北京时间）、会话切分（相邻有效消息间隔 ≥ 30 分钟为界，首位发言者为发起者）、回复速度（跨人首次回应，过滤 <60 秒连发和 >12 小时超长间隔，用中位数）。
- **文本**：jieba 分词 + 停用词 + 主题词典。**词云/高频词必须用 `jieba.posseg` 词性过滤**，只保留实词（n/nr/ns/nt/nz/vn/v/a/an/i/l/ng），否则会被「收到/看看/不能/不想」等口语功能词淹没。
- **阶段划分**：按月消息量 + 表情占比聚类（手写 KMeans 即可，无需 sklearn），保证时序连续，阶段数不必强凑 3-4。
- **之最**：消息最多的一天、最长连续会话、最长单条文字、最长语音、通话总时长等。

### 第 5 步：报告生成（.md）

报告结构（有数据、有判断、有结论）：

1. 总览（关键数字）
2. 聊了多少（量 + 类型构成）
3. 怎么聊（时间、会话、回复速度）
4. 聊什么（高频词、主题）
5. 变化（阶段划分）
6. 之最（可验证记录）
7. 总结（3-5 条克制的事实判断，不把数据解读为人格或感情高低）

每个数据段落后写「判断」，用数据支撑、克制表达，不臆测事件、不过度解读。

### 第 6 步：可选可视化（matplotlib / PPT）

- 配色用低饱和暖色系，双方各一个固定易区分颜色（如陶土橙 `#C9775A` + 沙金 `#D9A05B`）。
- 中文字体：`plt.rcParams['font.sans-serif'] = ['Microsoft YaHei', 'SimHei']`。
- PPT 用 `python-pptx`，每页「标题 + 图表 + 判断卡片」三段式。

### 第 7 步：HTML 祝福网页生成（核心增值能力）

当用户要求做成网页/礼物时，在 .md 报告基础上生成单文件 HTML（+ 资源文件夹），包含三大模块：**数据可视化报告 + 照片滚动故事线 + 互动结尾**。

#### 7.1 整体结构

```
封面（合照背景 + 蒙版 + 标题）
├── 数据可视化报告（7 个 Chart.js 交互图表，每个图表下附判断结论）
│   ├── 双方消息量饼图
│   ├── 内容构成环形图
│   ├── 每月走势双折线
│   ├── 24 小时活跃曲线
│   ├── 会话发起饼图
│   ├── 主题偏好柱状图
│   └── 亲密称呼演变折线
├── 关系阶段时间轴
├── 记录之最
├── 照片滚动故事线（全屏滚动，每幕一图+文字，时间轴+拍立得风格）
├── 互动结尾（生日蛋糕 / 纪念日彩蛋，点击触发撒花+打字机祝福）
└── 音乐播放器（右下角悬浮，可选）
```

#### 7.2 数据报告 HTML 化

- 用 **Chart.js**（CDN 引入）做交互图表，比 matplotlib 静态图更有体验。
- 7 个图表的数据直接从 `stats.json` 注入为 JS 变量。
- **每个图表下方必须有「📌 判断」结论框**，用数据支撑的克制判断，不写空话。
- 配色：紫 `#6366f1` + 粉 `#ec4899` 渐变主题，温馨浪漫。
- 人称约束：**全程用双方名字/昵称，禁止「我/你/他/我们/对方」等人称代词**（用户明确要求时执行）。

#### 7.3 照片故事线收集与排版

- **引导用户逐张发送照片 + 讲述背后的故事**，按时间顺序编号存档为 `story_01.png`、`story_02.png`……多图用 `story_04a.png`/`story_04b.png`。
- 每幕记录：幕号、标题、图片文件列表、故事文字。
- 无照片的幕用纯文字卡片 + 大字号装饰，不留空。
- 排版方案（按用户偏好选择）：
  - **时间轴+拍立得**（推荐）：左侧垂直时间线，右侧拍立得照片（白边+胶带+微旋转）+ 文字，表情贴纸随机撒在空白处
  - **左图右文**：图片在左、文字在右，适合竖图单图
  - **全屏背景+文字叠加**：图片铺满全屏做背景，文字用毛玻璃卡片叠加，沉浸感强但可能裁切人脸
- 图片必须 `object-fit: contain` 完整显示，**禁止裁切人脸**。

#### 7.4 互动元素（按需配置）

| 元素 | 实现方式 | 触发 |
|---|---|---|
| 鼠标跟随爱心 | `mousemove` 事件创建 emoji 元素，CSS 动画上浮淡出 | 鼠标移动 |
| 点击图片放大 | FLIP 动画，从原位置平滑放大到屏幕中央，再点返回 | 点击拍立得 |
| BGM 播放器 | 右下角圆形毛玻璃按钮，`<audio loop>`，播放/暂停图标切换 | 点击按钮 |
| 生日蛋糕 | CSS 画三层蛋糕+蜡烛火焰，点击吹灭→冒烟→撒花→打字机祝福 | 点击蛋糕 |
| 打字机祝福 | JS 逐字插入文本，带闪烁光标 | 蛋糕吹灭后自动 |
| 表情贴纸 | 20+ 张透明背景表情包，JS 随机绝对定位在各幕空白处，带浮动动画 | 页面加载 |
| 滚动淡入 | `IntersectionObserver`，内容滚入视口时淡入上移 | 滚动 |

#### 7.5 资源文件管理

- HTML 文件和所有资源（图片、音频）放在**同一目录**，HTML 用相对路径引用。
- 命名规范：`cover.jpg`（封面）、`ending.jpg`（尾页）、`story_XX.png`（故事图）、`sticker_XX.png`（表情）、`bgm.mp3`（背景音乐）。
- 交付时整个文件夹一起给用户，告知「打开 HTML 文件即可，资源需和 HTML 同目录」。

#### 7.6 HTML 模板使用

- 基础模板见 `templates/report_template.html`，包含完整结构、Chart.js 引入、CSS 样式、JS 交互框架。
- 使用时：替换数据变量、插入故事线 section、配置互动元素、替换资源路径。
- 模板中的 `/* === 数据注入区 === */` 和 `<!-- 故事线占位 -->` 是需要替换的位置。

## 关键参考

- 技术坑（jieba 安装、wordcloud 颜色、emoji 字体、误匹配等）见 `references/pitfalls.md`。
- HTML 生成相关坑（Chart.js 配置、图片裁切、互动动画、资源路径等）见 `references/html_pitfalls.md`。
- 通用分析脚本模板见 `scripts/analyze.py`。
- HTML 基础模板见 `templates/report_template.html`。

## 深入分析维度（按需扩展）

关系类聊天记录额外可做的分析（比全景统计更有「温度」）：

1. **亲密称呼演变**：找「宝宝/宝贝/亲爱的」等称呼的首次出现时间 + 谁先开口 + 按月频率曲线。
2. **表白节点**：找「我爱你/我喜欢你」首次出现时间、谁先说。
3. **冲突与和好**：识别强冲突信号（分手/拉黑/冷战/绝望），聚类出真冲突事件，算和好耗时（从爆发到道歉+亲密词恢复）。
4. **专属暗语**：挖双方都高频、但通用语料里少见的词。

注意「首次出现」要防误匹配：`《爱你》`是歌名、「比赛的对象」不是称呼、「别人的老婆」不是称呼，需人工剔除。

## 人称与隐私约束

- 若用户明确要求不用人称代词，全程使用双方名字/昵称，禁止「我/你/他/她/我们/你们/对方/我方」。
- 聊天记录含敏感信息（手机号、地址、身份证等）时，在报告和 HTML 中自动脱敏。
- HTML 网页默认本地交付，不上传任何服务器；若用户要求在线分享，需明确告知隐私风险。
